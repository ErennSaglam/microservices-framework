server:
  port: 8888

spring:
  application:
    name: config-server-git
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
